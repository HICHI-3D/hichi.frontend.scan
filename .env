# hichi.server (FastAPI 백엔드) 의 베이스 URL.
# 프론트가 /api/furniture/scan, /api/furniture/, SSE 스트림 등을 호출할 때 사용.
# .env 로 복사한 뒤 환경에 맞게 수정. 기본값(미설정 시) 도 http://localhost:8000 이다.
VITE_API_BASE_URL=http://localhost:8000
